apiVersion: v1
kind: Secret
metadata:
  name: oss-secret
  namespace: default # 保持与 NFS 一致，部署在 default 命名空间
type: Opaque
stringData:
  akId: "YOUR_ACCESS_KEY_ID"       # 替换为实际的 OSS AccessKey ID
  akSecret: "YOUR_ACCESS_KEY_SECRET" # 替换为实际的 OSS AccessKey Secret
